<form method="post" action="{{ path('app_singles_delete', {'id': single.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ single.id) }}">
    <button class="btn">Delete</button>
</form>
