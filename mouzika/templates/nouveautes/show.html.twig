{% extends 'base-back.html.twig' %}

{% block title %}Nouveautes{% endblock %}

{% block body %}
    <h1>Nouveautes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nouveaute.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ nouveaute.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ nouveaute.description }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ nouveaute.rating }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_nouveautes_index') }}">back to list</a>

    <a href="{{ path('app_nouveautes_edit', {'id': nouveaute.id}) }}">edit</a>

    {{ include('nouveautes/_delete_form.html.twig') }}
{% endblock %}
